# Retention policy

Retention is configured by jurisdiction, product, record type, legal hold, and the bank's approved schedule. Compliance records must use retention lock and legal-hold support. Deletion is prohibited while a retention lock or legal hold exists; deletion events themselves are auditable. The local JSONL store is a development substitute only.
